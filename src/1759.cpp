#include <bits/stdc++.h>
using namespace std;

int l, c;
char a[16];

int go(int start, int len){
    if (len == l);
}

int main(){
    cin >> l >> c;
    for (int i = 0 ; i < c ; i++){
        cin >> a[i];
    }
    for (int i = 0 ; i < c - l ; i++){
        go(i, 1);
    }
    return 0;
}
