#include <bits/stdc++.h>
using namespace std;

int n, m;
int a[51][51];

void solve(){

}

int main(){
    cin.sync_with_stdio(false);
    cin.tie(0);

    cin >> n >> m;
    for (int i = 0 ; i < n ; i++){
        for (int j = 0 ; j < n ; j++){
            cin >> a[i][j];
        }
    }
    solve();
    return 0;
}
