#include <iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--){
        int h, w, n;
        cin >> h >> w >> n;
        int x, y;
        x = y = 1;
        while(--n){

        }
        int ans = 100 * x + y;
        cout << ans << '\n';
    }
    return 0;
}
